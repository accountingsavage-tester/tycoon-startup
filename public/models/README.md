# Asset pipeline

Place optimized `.glb` low-poly models here. The game currently uses procedural low-poly geometry so the repo remains lightweight and deployable. Replace individual placeholder meshes with licensed CC0/compatible GLB assets as they are added.
