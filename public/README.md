# Startup Tycoon Assets

Drop CC0 or otherwise properly licensed low-poly `.glb` assets into `public/models/` and reference them from the Three.js scene. Keep models optimized for mobile: low polygon counts, compressed textures, and small file sizes.
